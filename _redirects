# Cloudflare Pages redirects — https://developers.cloudflare.com/pages/configuration/redirects/
# SPA fallback: serve index.html for any path so the hash router can take over.
# Existing static files and /functions routes are matched BEFORE this rule,
# so this only catches unknown paths.
/*  /index.html  200
